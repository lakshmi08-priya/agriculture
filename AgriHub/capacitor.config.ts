import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Farmer.agrihub',
  appName: 'KrishiSetu',
  webDir: 'www', // <- must match the folder containing index.html
};

export default config;
